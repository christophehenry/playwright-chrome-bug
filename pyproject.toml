[project]
name = "playwright-chrome-bug"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.2",
    "pytest>=9.0.2",
    "pytest-httpserver>=1.1.3",
    "pytest-playwright>=0.7.2",
]
